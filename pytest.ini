# pytest.ini
[pytest]
minversion = 7.0
addopts = -q
testpaths = tests
markers =
    integration: marks tests as integration (slow / require heavy deps)
filterwarnings =
    ignore::DeprecationWarning
    